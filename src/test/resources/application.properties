# Test-only config. Don't auto-start the RabbitMQ listener during tests, so the
# context-loads test passes without a real RabbitMQ broker running (e.g. in CI).
spring.rabbitmq.listener.simple.auto-startup=false
